# install git 
# creaate represotory in git hub 
# go t o git bash
# git config --global user.name "username"
# git config --global user.email "email"

# git init
# git status => to check the status of the files
# git diff => to check the difference between the files
# git add . => to add all the files to the staging area
# git commit -m "message" => to commit the files to the repository
# copy paste git code from github and paste it in git bash


########
code change
git add . 
git commit -m "initial commit"
git push 
########


